[package]
name = "wiremesh-enforcer-program"
version = "0.1.0"
edition.workspace = true

license.workspace = true

[dependencies]
wiremesh-enforcer-common = { path = "../common" }

aya-ebpf = { workspace = true }
aya-log-ebpf = { workspace = true }

[build-dependencies]
which = { workspace = true }

[[bin]]
name = "wiremesh-enforcer"
path = "src/main.rs"

[lib]
path = "src/lib.rs"
